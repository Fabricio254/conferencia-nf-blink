# .streamlit/secrets.toml — Exemplo para Streamlit Cloud
# Copie este arquivo para ~/.streamlit/secrets.toml no seu ambiente local
# OU use a interface do Streamlit Cloud para adicionar os secrets

# ─── API FASTAPI ─────────────────────────────────────────────────────────────
api_url = "http://seu-servidor.com:8000"
api_token = "seu_token_super_secreto_aqui"

# ─── DATABASE (se necessário para acesso direto) ─────────────────────────────
[database]
host = "192.168.15.240"
port = 3051
user = "SYSDBA"
password = "masterkey"
database = "C:\\Program Files (x86)\\VisualControl\\Database\\ERP_VCONTROL_BLINK.FDB"

# ─── EMAIL ─────────────────────────────────────────────────────────────────
[email]
remetente = "blinkcpd@gmail.com"
senha_app = "sua_senha_de_app"
destinatario = "contabilidade@empresa.com.br"
brevo_api_key = "sua_chave_brevo"

# ─── LOJAS ──────────────────────────────────────────────────────────────────
[[lojas]]
nome = "FRAMODAS"
loja_id = 1
cnpj = "27273028000159"

[[lojas]]
nome = "MAD ATHOS FL2"
loja_id = 2
cnpj = "04665291713"

[[lojas]]
nome = "SANTEL"
loja_id = 10
cnpj = "03248221000171"
